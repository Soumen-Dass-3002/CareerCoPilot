import { Link, useParams } from "react-router-dom";
import { getCareer } from "../data/careers";
import "./careers.css";
function CareerDetail() { const { careerId } = useParams(); const career = getCareer(careerId); if (!career) return <div className="not-found"><h1>Career not found</h1><Link to="/careers">Explore careers</Link></div>; return <div className="careers-page"><header className="explore-header"><Link to="/careers" className="back-dashboard">← Explore careers</Link><Link to="/" className="explore-brand">✦ Career<span>CoPilot</span></Link></header><main className="detail-main"><p className="eyebrow">{career.domain.toUpperCase()} CAREER</p><h1>{career.name}</h1><p className="detail-lede">{career.description}</p><div className="detail-actions"><Link to={`/compare?careers=${career.id},software-engineer`}>Compare this career</Link><Link className="soft-link" to="/careers">Browse all careers</Link></div><section className="detail-grid"><div className="detail-card wide"><h2>What this career involves</h2><p>{career.description} The specific work can differ by organisation, role, and the problems being solved.</p><h3>Who it may suit</h3><p>It can be a good option if you enjoy {career.fit.join(", ")}. This is information to help you explore, not a prediction about what you should choose.</p></div><div className="detail-card"><h2>Important skills</h2><ul>{career.skills.map((item) => <li key={item}>{item}</li>)}</ul></div><div className="detail-card"><h2>Education paths</h2><ul>{career.education.map((item) => <li key={item}>{item}</li>)}</ul><h3>Relevant branches</h3><p>{career.degrees.join(" · ")}</p></div><div className="detail-card"><h2>Typical entry roles</h2><ul>{career.roles.map((item) => <li key={item}>{item}</li>)}</ul></div><div className="detail-card wide"><h2>A possible roadmap</h2><ol className="roadmap">{career.roadmap.map((step, index) => <li key={step}><span>{index + 1}</span>{step}</li>)}</ol><p className="roadmap-note">Your path can be different. Use this as a starting map and adapt it to your situation.</p></div></section></main></div>; }
export default CareerDetail;
