import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { profileService } from "../services/storage";
import "./Auth.css";
const Loop=()=> <span className="auth-loop"><i/><i/></span>;
function Signup(){const navigate=useNavigate();const[name,setName]=useState("");const[email,setEmail]=useState("");const submit=e=>{e.preventDefault();profileService.save({...profileService.get(),name,email});localStorage.setItem("cc-auth-popup-dismissed","true");navigate("/")};return <div className="auth-page"><section className="auth-intro"><Link to="/" className="auth-brand"><Loop/>career<span>copilot</span></Link><div className="auth-orbit"><i/><i/></div><p className="eyebrow">YOUR FUTURE, YOUR WAY</p><h1>Let curiosity<br/>lead the <em>way.</em></h1><p>There’s no test to pass here — just a better way to learn about yourself and the possibilities ahead.</p><div className="intro-trust">Join thousands of students exploring their future</div></section><section className="auth-form-side"><div className="auth-box"><p className="auth-kicker">START EXPLORING</p><h2>Create your account</h2><p className="auth-subtitle">It’s free, and takes less than a minute.</p><form onSubmit={submit}><label>Your name<input value={name} onChange={e=>setName(e.target.value)} type="text" placeholder="What should we call you?" required/></label><label>Email address<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" required/></label><label>Password<input type="password" placeholder="Create a password" required/></label><button className="primary-auth" type="submit">Create my account <span>Continue</span></button></form><div className="or"><span/>or continue with<span/></div><button className="provider" type="button" onClick={()=>navigate("/")}><b>G</b>Sign up with Google</button><p className="terms">By continuing, you agree to our Terms and Privacy Policy.</p><p className="auth-switch">Already have an account? <Link to="/login">Log in</Link></p></div></section></div>}
export default Signup;
